Data Transfar between one fragment to other fragment
====================================================

1. create argument in receive fragment (auto java generated by compiler)

2.	sender faragment :
		btnNext.setOnClickListener{
           var action = OneFragmentDirections.actionOneFragmentToTwoFragment("message")

            findNavController().navigate(action)
            
        }
		
3. 	receiver faragment
		 var 	 = arguments?.let {
            TwoFragmentArgs.fromBundle(it)
        }
        var message:String = messageArgs?.message.toString()

        txtTwoFragment.text = message